/* EXAM REVIEW
 * Part 1: multiple choice/fill in the blank
 * Part 2: convert equation into C expression 
 * 
 * MUST KNOW:
 * + casting
 * + variable naming rules
 * + a bit about scanf() -- reads up to whitespace
 *   formatting
 * + declaring a string (array of chars,
 *                       max number chars + 1 for EOF)
 * + gets() for strings with spaces
 * + flushing -- getchar(); or fflush(stdin);
 * + printf();
 * 
 * PART 2:
 * Similar to homework, must create own variable names
 * or use givens. Must also know how to set data types, etc.
 * 
 * ALSO: on homework, RUN CODE(S) FIVE TIMES EACH [except stocks]
 */

#include <stdio.h>

int conversion()
{
    int a, b, c, d, k;
    // EXP: a(b+d) / 4(kd+bd)
    c = (a * b + a * d) / (4 * k * d + 4 * b * d);
    // MAKE SURE THIS IS A C EXPRESSION!
    return 0;
}